apiVersion: v1
kind: Service
metadata:
  namespace: songs-service
  name: db
  labels:
    app: songs-service
    type: db

spec:
  selector:
    app: songs-service
    type: db
  ports:
    - name: songs-service-db-listener
      protocol: TCP
      port: 5432
      targetPort: 5432
